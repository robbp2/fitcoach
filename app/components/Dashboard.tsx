// Legacy Dashboard component — superseded by app/dashboard/page.tsx (Phase 4).
// Kept as a stub to avoid import errors during the build transition.
// This file is not imported or rendered by any route.

export default function LegacyDashboard() {
  return null;
}
